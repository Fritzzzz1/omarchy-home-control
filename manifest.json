{
  "schemaVersion": 1,
  "id": "omarchy.home-control",
  "name": "Omarchy Home Control",
  "version": "1.0.0",
  "author": "Liran Friedman",
  "license": "MIT",
  "description": "Talk to the agent on this machine from your phone. Adds a bar widget for the voice channel's service, and ships the installer for the service itself.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Home Control",
    "description": "Shows whether the voice channel is up; click to open the phone URL, right-click to start it when it is down.",
    "category": "System",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "unit": "home-control.service",
      "refreshIntervalSec": 10,
      "showLabel": false
    },
    "schema": [
      {
        "key": "unit",
        "type": "string",
        "label": "systemd user unit",
        "defaultValue": "home-control.service"
      },
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Status poll interval (seconds)",
        "min": 2,
        "max": 600,
        "step": 1,
        "defaultValue": 10
      },
      {
        "key": "showLabel",
        "type": "boolean",
        "label": "Show the word \"Home Control\" next to the icon",
        "defaultValue": false
      }
    ]
  }
}
